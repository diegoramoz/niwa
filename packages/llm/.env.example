# Local proxy port that forwards to Ollama's default port (11434)
LLM_PROXY_PORT=7002

# Token from `cloudflared tunnel token <tunnel-name>`
CF_TUNNEL_TOKEN=

# Public hostname of the tunnel (e.g. llm.example.com) — printed on startup
CF_TUNNEL_HOSTNAME=
